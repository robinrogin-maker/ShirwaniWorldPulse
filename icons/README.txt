Simple generic line icons (SVG, use currentColor) - one per section.
Safe to replace with your own icon files any time; just keep the same
filenames referenced in data.js, or update the ICONS map in data.js.
