{
  // Cloudflare Pages configuration for the deployed TanStack Start app.
  // Runtime secrets (SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, ...) are NOT listed here
  // on purpose: values added as *Secrets* in the Cloudflare dashboard are preserved
  // across deploys, while plain-text `vars` in this file would overwrite them.
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "worldspectrum",
  "pages_build_output_dir": "dist",
  "compatibility_date": "2025-06-01",
  "compatibility_flags": ["nodejs_compat"],
  "observability": {
    "enabled": true
  }
}
