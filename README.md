# ShirwaniWorldPulse

واجهة إخبارية عربية متعددة الأقسام، مبنية باستخدام HTML وCSS وJavaScript فقط.

## التشغيل

افتح `index.html` مباشرة في المتصفح، أو شغّل خادماً محلياً بسيطاً:

```bash
python3 -m http.server 8000
```

ثم انتقل إلى <http://localhost:8000>.